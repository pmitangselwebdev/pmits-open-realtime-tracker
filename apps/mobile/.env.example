# Mobile App — Open Realtime Tracker
# ====================================
# App configuration is done IN the app (Server URL + Unique ID).
# No env vars required for local development.
#
# For EAS Build, you can set:
#   EXPO_PUBLIC_DEFAULT_URL=https://your-app.vercel.app
# (optional, used as placeholder in setup screen)
